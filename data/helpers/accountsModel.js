const db = require('../dbConfig.js');

module.exports = {
  get,
  insert,
  update,
  remove,
};

function get(id) {};

function insert(account) {};

function update(id, accountChanges) {};

function remove(id) {};
